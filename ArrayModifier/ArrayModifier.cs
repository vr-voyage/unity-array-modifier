using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Myy
{
public class ArrayModifier : MonoBehaviour
{
    public Mesh meshToReplicate;
    public int nTimes;
    public Vector3 cloneOffsetPosition = Vector3.zero;
    public Vector3 cloneOffsetRotation = Vector3.zero;
    public Vector3 cloneOffsetScale = Vector3.one;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

}
